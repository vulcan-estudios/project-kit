var chai = require('chai');
var assert = chai.assert;

describe('component', function () {

    it('component feature should do something', function () {
        var ok = 'is ok';

        assert.equal(!!'everything', !!ok);
    });
});
